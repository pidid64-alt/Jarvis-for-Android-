<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="kz.jarvis.app"
    android:versionCode="11"
    android:versionName="2.6.0">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <!-- «звонок всегда с первой SIM»: читаем список SIM-карт -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- автоответчик: «где я» по запросу собеседника -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- эффект «брони» для голоса (эхо поверх речи) -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- сохранение презентаций в «Загрузки» на Android 8-9 (10+ работает через MediaStore) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- «кто звонил последним», «перезвони» -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <!-- «какой у меня номер» (номер SIM-карты) -->
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <!-- «яркость 40 процентов»: выдаётся пользователем в системном окне -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <!-- напоминания в точное время -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- «поверх других окон»: сфера над экраном + запуск окон из фона -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- чтобы система не выгружала службу ожидания -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="34" />

    <!-- Видимость установленных приложений (Android 11+) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- «отнеси материал в Клод» -->
        <package android:name="com.anthropic.claude" />
        <!-- музыка/видео и мессенджеры: спотифай, ютуб, вацап, телеграм -->
        <package android:name="com.spotify.music" />
        <package android:name="com.google.android.youtube" />
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <package android:name="org.telegram.messenger" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <application
        android:name=".App"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.Jarvis">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings" />

        <!-- Фоновое ожидание слова «Джарвис».
             stopWithTask=false — служба живёт, даже когда приложение смахнули. -->
        <service
            android:name=".WakeWordService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="microphone" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".ReminderReceiver"
            android:exported="false" />

        <!-- Автоответчик: читает входящие уведомления мессенджеров и отвечает.
             Разрешение «доступ к уведомлениям» пользователь выдаёт в системных
             настройках (кнопка в Настройках Джарвиса → «Автоответчик»). -->
        <service
            android:name=".AutoReplyService"
            android:label="Джарвис — автоответчик"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Автоотправка в WhatsApp: жмёт «Отправить» за пользователя, когда
             он сказал «напиши маме в вацап…». Включается один раз в системных
             «Спец. возможностях» (кнопка в Настройках Джарвиса). -->
        <service
            android:name=".AutoSendService"
            android:label="@string/auto_send_a11y_label"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_auto" />
        </service>
    </application>
</manifest>
