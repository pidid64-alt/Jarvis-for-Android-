# 🤖 J.A.R.V.I.S. для Android

Голосовой ассистент в стиле Железного человека: wake-word «Джарвис», офлайн-управление
телефоном, чат с Google Gemini и озвучка ответов. Всё на русском.

![Скриншот-идея](icon_master.png)

## 🆕 Что нового в 2.1

- **≈200 офлайн-команд** вместо ~90: добавлены единицы измерения (длина, масса,
  объём, площадь, скорость, мегабайты, время, температура), бытовые расчёты
  (ИМТ, чаевые, скидка, деление счёта, площадь комнаты, факториал, НОД/НОК,
  римские числа, знак зодиака), календарь («сколько дней до 31 декабря»,
  «какой день недели 1 января 2030», «сколько мне дней»), команды для текста
  («сколько слов», «переверни фразу», «морзе», «транслит»), списки покупок и
  заметки, состояния телефона (IP, оператор, экран, процессор, датчики,
  блютуз/вайфай/режим полёта), яркость экрана, громкость по потокам,
  журнал звонков, установка и удаление приложений, почта и «поделиться».
- **Курсы валют** без ключей: «курс доллара», «100 долларов в тенге», «курс валют».
- **Справка по темам**: «команды конвертера», «команды телефона», «календарные
  команды», «команды для текста», «команды-развлечения», «команды списков».
- **Тестов втрое больше**: `scripts/test-logic.sh` — 132 проверки чистой логики,
  новый `scripts/test-commands.sh` — 49 проверок настоящего диспетчера команд
  (ловит конфликты разделов: «раздели 100 на 4» ≠ «раздели 9000 на троих»).
- **Попутно починено**: шаблоны с `\w` не работали с кириллицей — «мобильные
  данные», «максимальная громкость», «таймеры на 5 минут» теперь распознаются.

## 🆕 Что нового в 2.0

- **Работает, когда приложение закрыто.** Услышав «Джарвис», ассистент теперь сам
  слушает команду, выполняет её и отвечает голосом — окно открывать не нужно.
  Поверх экрана появляется сфера-индикатор с текстом ответа.
- **~90 офлайн-команд** вместо ~30: калькулятор, проценты, конвертер, погода,
  напоминания, управление плеером, громкость в процентах, память, модель телефона,
  время в другом городе, перевод, картинки, новости, Википедия, монетка/кубик/анекдот.
- **Напоминания без интернета**: «напомни через 20 минут выключить духовку» —
  сработает будильником, уведомлением и голосом, даже если приложение закрыто.
- **Раздел «Чтобы работало без приложения»** в настройках: одним нажатием выдаются
  нужные разрешения и показан чек-лист состояния.
- **Тесты чистой логики** (`scripts/test-logic.sh`) — проверки на JVM.

## ✨ Что умеет

### Работает без интернета (мгновенно)
| Команда | Действие |
|---|---|
| «включи / выключи **фонарик**» | Управление вспышкой |
| «**громче** / тише», «**громкость 40 процентов**», «на всю», «сколько сейчас громкость» | Громкость |
| «**пауза**», «**следующий трек**», «предыдущий трек», «продолжи музыку», «что играет» | Управление плеером (медиа-клавиши) |
| «**включи музыку** <название>» | Поиск и запуск в вашем плеере |
| «**открой** ютуб / телеграм / камеру / гугл плей / настройки…» | Запуск приложений (60+ псевдонимов) |
| «**позвони** маме», «позвони по номеру +7…», «**напиши** папе что задержусь» | Звонок / SMS по контактам |
| «**будильник на 7:30**», «разбуди в 6 вечера», «покажи будильники» | Будильник |
| «**таймер на 5 минут**», «через 30 секунд» | Таймер |
| «**напомни через 20 минут** выключить духовку», «напомни в 18:30 позвонить маме» | Свои напоминания (будильник + уведомление + голос) |
| «список напоминаний», «отмени напоминания» | Управление напоминаниями |
| «**сколько будет 12 умножить на 7**», «2+2*3», «**20 процентов от 150**», «корень из 144», «5 в квадрате», «2 в степени 10» | Калькулятор |
| «**100 фаренгейта в цельсиях**», «5 км в милях», «70 кг в фунтах», «180 см в дюймах» | Конвертер |
| «**сколько заряда**», «**сколько памяти**», «какая у меня модель», «версия андроид» | Состояние телефона |
| «**который час**», «какое число», «**время в Лондоне**», «сколько дней до нового года» | Время и дата |
| «**маршрут до** дома», «где находится Эрмитаж» | Навигация и карта |
| «**найди** пиццу», «**переведи** hello», «картинки котов», «новости», «википедия физика», «… на ютубе» | Интернет |
| «**сделай фото**», «сними видео» | Камера |
| «включи вайфай», «блютус», «авиарежим», «nfc», «мобильные данные», «не беспокоить», «энергосбережение», «настройки звука / экрана / приложений» | Системные панели |
| «**подбрось монетку**», «кинь кубик», «случайное число от 1 до 100», «расскажи анекдот», «цитата» | Всякое |
| «**спи 10 минут**», «выключи прослушивание» | Управление фоновой службой |
| «**фонарик на 10 секунд**», «мигни фонариком» | Фонарик с таймером |
| «**яркость 40 процентов**», «сделай ярче/темнее», «какая яркость» | Яркость экрана |
| «**громкость звонка / будильника / уведомлений 50 процентов**» | Громкость по потокам |
| «**перемотай вперёд / назад**» | Перемотка плеера |
| «**мой вес 80 рост 180**» (ИМТ), «**чаевые 10 процентов от 3500**», «**скидка 20 процентов с 5000**», «раздели 9000 на троих» | Бытовые расчёты |
| «**площадь комнаты 3 на 4**», «площадь круга радиус 5», «среднее 4 5 6», «факториал 5», «нод 12 и 18», «**1994 римскими**» | Ещё калькулятор |
| «**2 гб в мб**», «3 дня в часах», «**100 км/ч в м/с**», «1 гектар в акрах», «2 литра в галлонах», «6 соток в м2», «0 кельвинов в цельсиях» | Конвертер (8 групп величин) |
| «**курс доллара**», «**100 долларов в тенге**», «курс валют» | Курсы валют (нужен интернет) |
| «**сколько дней до 31 декабря**», «сколько дней между 1 мая и 20 мая», «**какой день недели 1 января 2030**», «какая дата через 40 дней», «сколько мне дней, если я родился 5 мая 1990», «сколько дней в феврале», «високосный ли 2028 год», «сколько секунд в сутках», «какое число завтра» | Календарь |
| «**какой у меня ip**», «какой оператор», «сколько оперативки», «какой процессор», «разрешение экрана», «какие датчики», «блютуз/вайфай/режим полёта включен», «здоровье батареи», «сколько приложений установлено», «сколько работает телефон» | Состояние телефона |
| «**добавь в список покупок молоко, хлеб**», «что в списке покупок», «удали молоко из списка покупок», «**запиши заметку** …», «мои заметки» | Списки и заметки (офлайн) |
| «**сколько слов в тексте** …», «сколько букв в …», «**переверни фразу** …», «заглавными …», «**морзе** привет», «**транслит** привет», «повтори за мной …» | Работа с текстом |
| «**кто звонил последним**», «последний пропущенный», «перезвони» | Журнал звонков |
| «**отправь письмо на** mail@example.com …», «**поделиться** …», «напиши в вотсап …» | Почта и отправка |
| «**установи приложение** шахматы», «**удали приложение** …», «мои приложения», «информация о приложении …» | Play Market |
| «открой **режим разработчика** / vpn / обои / загрузки / о телефоне / аккаунты / геолокацию» | Ещё панели настроек |
| «**открой сайт** example.com», «**переведи на английский** …» | Интернет |
| «**выбери между** чаем и кофе», «брось 3 кубика», «кубик на 20 граней», «да или нет», «правда или ложь», «**загадай загадку**» → «ответ на загадку», «интересный факт», «скороговорка», «комплимент», «**придумай пароль**», «случайный код из 6 цифр» | Развлечения и генераторы |
| «**стоп**», «**повтори**», «очисти историю», «**что ты умеешь**», «**команды конвертера**», «команды телефона», «календарные команды», «команды для текста», «команды-развлечения», «команды списков» | Служебное и справка по темам |

### Через Gemini (нужен бесплатный API-ключ)
Любые вопросы и беседа — контекст диалога сохраняется (общая история у окна и у
фоновой службы), ответы озвучиваются.
Получить ключ бесплатно: **https://aistudio.google.com/apikey**

## 🎙 Работа без приложения

1. Включите тумблер **«Служба „Джарвис"»** (включён по умолчанию) — появится
   постоянное уведомление «Джарвис на связи».
2. Скажите **«Джарвис»** — прозвучит сигнал, телефон вибрирует, поверх экрана
   появится сфера.
3. Произнесите команду — Джарвис выполнит её и ответит голосом. Приложение при
   этом открывать **не нужно**.
4. Если команда требует экрана (звонок, запуск чужого приложения) — окно откроется
   само, но только при включённом разрешении «Показ поверх других окон».

**Настройки ⚙ → «Чтобы работало без приложения»** — чек-лист из шести пунктов:
ожидание слова, микрофон, уведомления, поверх других окон, без ограничений батареи,
точные напоминания. Зелёные галочки = всё готово.

### Честно об ограничениях Android

- **Android 14+** запрещает запускать микрофон-службу из фона, в том числе по
  `BOOT_COMPLETED`. Поэтому **после перезагрузки** приходит уведомление
  «Джарвис не слушает — нажмите, чтобы включить»: одно нажатие открывает окно,
  и служба запускается уже легально.
- Без разрешения **«Показ поверх других окон»** Android 10+ не даст открыть окно
  (звонок, YouTube) из закрытого приложения — голосовые ответы при этом работают.
- Без **«Не ограничивать работу в фоне»** прошивки (Xiaomi, Samsung, Huawei)
  выгружают службу через несколько минут.
- Wake-служба использует системный `SpeechRecognizer`: на устройствах без сервиса
  распознавания речи (без Google-приложения) она работать не будет — в уведомлении
  появится соответствующая надпись.

## 📲 Установка

1. Скачайте APK из этого репозитория:
   **[releases/Jarvis-v2.1.apk](releases/Jarvis-v2.1.apk)**
   (на странице файла нажмите «Download raw file»).
2. Разрешите установку из неизвестных источников.
   > Ставите поверх старой версии и видите «подписи не совпадают»? Удалите старую —
   > debug-ключ (`keystore/`) в Git не хранится и создаётся заново при сборке.
3. При первом запуске дайте разрешения: **микрофон** (обязательно), уведомления,
   контакты (для «позвони…»).
4. Настройки ⚙ → раздел **«Чтобы работало без приложения»** → выдайте три разрешения.
5. Настройки ⚙ → API-ключ Gemini → «Проверить ключ» (для свободных вопросов).
6. Готово: скажите «Джарвис» или нажмите на сферу-реактор 💠 в приложении.

> Приложение не собирает и не отправляет ничего, кроме текста ваших запросов в Google
> Gemini (и только если вы вставили ключ). API-ключ хранится только на телефоне.

## 🛠 Сборка из исходников

### Способ 1 — оффлайн-скрипт (без Gradle и Android SDK)
Тулчейн (JDK, build-tools, android.jar, kotlin-compiler) качается из публичных
Git-зеркал AOSP — работает даже там, где Maven Central заблокирован:

```bash
scripts/fetch-toolchain.sh   # один раз, нужно право писать в /opt
scripts/build-apk.sh         # соберёт releases/Jarvis-v<версия>.apk
scripts/test-logic.sh        # 132 проверки чистой логики (быстро)
scripts/test-commands.sh     # 49 проверок диспетчера команд (компилирует всё приложение)
```

Пайплайн: `aapt2 compile/link → kotlinc → d8 (+ kotlin-stdlib) → zipalign → apksigner`.
Подпись: самоподписанный debug-ключ `keystore/jarvis.jks` (создаётся автоматически,
пароль `jarvis25`). Для публикации замените на свой ключ!

`scripts/test-logic.sh` компилирует **те же самые файлы**, что попадают в APK
(`MathEngine.kt`, `WakeWords.kt`, `TimeParse.kt`, `Timezones.kt`, `Units.kt`,
`LifeCalc.kt`, `DateFacts.kt`, `TextTools.kt`, `Randoms.kt`), и прогоняет их на
обычной JVM — эмулятор не нужен.

`scripts/test-commands.sh` идёт дальше: компилирует **всё приложение** (с
автогенерацией R-класса через aapt2) и дёргает настоящий `CommandEngine.execute()`
на JVM с Context-«пустышкой». Так проверяется порядок разделов — например, что
«раздели 100 на 4» остаётся арифметикой, а «раздели 9000 на троих» делит счёт.

### Способ 2 — Android Studio / Gradle
Проект обычный: `app/`, `compileSdk 34`, `minSdk 26`, Kotlin, **нулевые зависимости**.
```bash
./gradlew assembleDebug   # или откройте папку в Android Studio
```
> Если ваш AGP ругается на `package=` в манифесте — удалите эту строку
> (namespace уже задан в `app/build.gradle.kts`). Оффлайн-скрипту она нужна.

## 📁 Структура

```
app/
  AndroidManifest.xml        — разрешения, службы, приёмники (boot, напоминания)
  res/                       — ресурсы (тёмная cyan-тема, иконки-векторы, mipmaps)
  src/kz/jarvis/app/
    App.kt                   — Application, каналы уведомлений, «видно ли окно»
    MainActivity.kt          — чат, распознавание речи, оркестрация
    CommandEngine.kt         — диспетчер офлайн-команд: порядок разделов, справка
    WakeWordService.kt       — фоновая служба: ждёт «Джарвис», слушает и выполняет команду
    OverlayController.kt     — плавающая сфера поверх других приложений
    ReminderScheduler.kt     — напоминания через AlarmManager
    ReminderReceiver.kt      — срабатывание напоминания (уведомление + голос)
    MathEngine.kt            — калькулятор (чистый Kotlin, тестируется на JVM)
    WakeWords.kt             — распознавание слова «Джарвис» (чистый Kotlin)
    TimeParse.kt             — «через 10 минут» / «на 7:30» (чистый Kotlin)
    Timezones.kt             — часовой пояс по названию города
    Units.kt                 — конвертер: 8 групп величин (чистый Kotlin)
    LifeCalc.kt              — ИМТ, чаевые, скидка, площадь, НОД, зодиак (чистый Kotlin)
    DateFacts.kt             — календарные вопросы (чистый Kotlin)
    TextTools.kt             — слова/буквы, переворот, Морзе, транслит (чистый Kotlin)
    Randoms.kt               — монетка, кубики, загадки, пароль (чистый Kotlin)
    SystemCommands.kt        — состояние телефона, яркость, маркет, журнал звонков
    Lists.kt                 — список покупок и заметки (SharedPreferences)
    Rates.kt                 — курсы валют через open.er-api.com, без ключей
    Weather.kt               — погода через wttr.in, без ключей
    Conversation.kt          — общая история диалога окна и службы
    GeminiClient.kt          — REST-клиент Gemini (JSON, без библиотек)
    SettingsActivity.kt      — ключ, модель, тумблеры, разрешения фона
    TtsController.kt         — озвучка (TextToSpeech, ru-RU)
    Speaker.kt               — одноразовая озвучка из BroadcastReceiver
    Notify.kt                — каналы и уведомления
    OrbView.kt               — анимированная сфера-реактор (Canvas)
    Prefs.kt, BootReceiver.kt
scripts/
  fetch-toolchain.sh         — скачивание тулчейна из зеркал
  build-apk.sh               — полный оффлайн-пайплайн сборки APK
  test-logic.sh              — тесты чистой логики на JVM (132 проверки)
  test-commands.sh           — тесты диспетчера команд (49 проверок)
tools/
  LogicTest.kt               — проверки калькулятора/wake-слова/времени/конвертера…
  CommandTest.kt             — проверки CommandEngine.execute() на JVM
```

## ⚠️ Замечания

- Требуется Android **8.0+** (API 26). Проверено на targetSdk 34.
- Курсы валют и погода требуют интернет; курсы обновляются сервисом раз в сутки.
- «Яркость 40 процентов» попросит разрешение «изменять системные настройки»
  (Android не даёт менять яркость без него) — выдаётся одним нажатием.
- «Кто звонил последним» и «какой у меня номер» работают только после выдачи
  соответствующих разрешений.
- Постоянное прослушивание микрофона расходует батарею — скажите
  **«Джарвис, спи 60 минут»** или выключите тумблер.
- На некоторых прошивках сервис распознавания недоступен — тогда отключите
  wake-службу (иконка 🎙 в шапке) и пользуйтесь окном.
- Лицензия проекта — **GPL-3.0** (унаследована).

**С возвращением, сэр.** 🎩
